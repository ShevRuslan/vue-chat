export default {
  framework: {
    components: ['QCard', 'QCardSection', 'QCardActions, QInput, QForm, QBtn']
  },
  plugins: ['Notify'],
  config: {
    notify: {
      /* Notify defaults */
    }
  }
};
